namespace Shield.Enums
{
    /// <summary>
    /// Type of incoming message enum
    /// </summary>
    public enum IncomingMessageType
    {
        Master,
        Slave,
        Confirmation,
        Undetermined
    }
}